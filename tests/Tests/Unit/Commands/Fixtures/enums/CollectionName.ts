export enum CollectionName {
    Category = "categories",
    CategoryPost = "category_post",
    Post = "posts",
    Secret = "secrets",
    User = "users",
}

export type CollectionNameEnum = `${ CollectionName }`;
