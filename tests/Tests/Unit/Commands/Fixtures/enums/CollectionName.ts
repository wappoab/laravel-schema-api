export enum CollectionName {
    Category = "categories",
    CategoryPost = "category_posts",
    Post = "posts",
    Secret = "secrets",
    User = "users",
}

export type CollectionNameEnum = `${ CollectionName }`;
